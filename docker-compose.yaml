version: '3.8'
services:
  frontend: 
    container_name: frontend
    build: "."
    ports:
      - 3000:3000
    volumes:
      - '/usr/app'
      - '/usr/app/node_modules'
    environment:
      - CHOKIDAR_USEPOLLING=true
      